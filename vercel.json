{
  "buildCommand": "npx turbo run build --filter=@unipath/web && cp -r apps/web/dist .",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
