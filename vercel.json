{
  "rootDirectory": "apps/web",
  "buildCommand": "cd ../.. && npx turbo run build --filter=@unipath/web",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
